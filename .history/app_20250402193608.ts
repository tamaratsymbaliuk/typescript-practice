const num1Input = document.getElementById('num1');
const num2Input = document.getElementById('num2');
const buttonElement = document.query

function add(a: number, b: number) {
  return a + b;
}

const result = add(5, 3);

console.log(result);